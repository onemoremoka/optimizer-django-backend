apiVersion: 1

providers:
  - name: 'Docker Monitoring'
    orgId: 1
    folder: 'Docker'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards

      #(.venv) marcosignacio@notebook:~/Desktop/repos/optimizer-django-backend$ sed -i 's/\${DS_PROMETHEUS}/Prometheus/g' deployments/grafana/metric_dashboard.json
